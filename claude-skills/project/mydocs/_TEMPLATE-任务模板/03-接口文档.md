# 接口文档

## 接口1: [接口名称]

### 基本信息
- **接口路径**: `POST /api/xxx/xxx`
- **接口描述**: [接口功能说明]
- **是否需要权限**: 是/否
- **权限要求**: [如需要,说明权限类型]

### 请求参数
```json
{
  "param1": "String, 参数说明",
  "param2": "Long, 参数说明",
  "param3": {
    "subParam1": "参数说明"
  }
}
```

**参数校验规则:**
- `param1`: 必填, 最大长度255
- `param2`: 必填, 大于0

### 响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "field1": "String, 字段说明",
    "field2": "Long, 字段说明",
    "field3": {
      "subField1": "字段说明"
    }
  }
}
```

### 异常情况
- **错误码1001**: [错误说明]
- **错误码1002**: [错误说明]

### 调用示例
```bash
curl -X POST 'http://localhost:8080/api/xxx/xxx' \
  -H 'Content-Type: application/json' \
  -d '{
    "param1": "value1",
    "param2": 123
  }'
```

---

## 接口2: [如有多个接口,按上述格式继续添加]

...
