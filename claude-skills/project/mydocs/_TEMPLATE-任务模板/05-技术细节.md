# 技术细节

## 需要调用的外部接口
> 📚 参考知识库: `knowledge-base/external-services/[相关服务].md`

### 外部接口1: [接口名称]
- **服务名称**: [服务名]
- **接口路径**: [路径]
- **调用方式**: [SOA/HTTP/RPC]
- **请求参数**: [参数说明]
- **响应格式**: [响应说明]
- **异常处理**: [异常处理策略]

### 外部接口2: [如有多个,继续添加]
...

---

## 数据库设计

### 新增表 [如需要]

**表名**: `table_name`

**字段设计**:
```sql
CREATE TABLE `table_name` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
  `field1` VARCHAR(255) NOT NULL COMMENT '字段说明',
  `field2` INT DEFAULT 0 COMMENT '字段说明',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除标记(0-未删除,1-已删除)',
  PRIMARY KEY (`id`),
  KEY `idx_field1` (`field1`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='表说明';
```

**索引设计**:
- 主键索引: `id`
- 普通索引: `idx_field1` - [索引用途说明]

---

### 修改表 [如需要]

**表名**: `existing_table_name`

**新增字段**:
```sql
ALTER TABLE `existing_table_name`
ADD COLUMN `new_field` VARCHAR(255) DEFAULT NULL COMMENT '字段说明';
```

**新增索引**:
```sql
ALTER TABLE `existing_table_name`
ADD INDEX `idx_new_field` (`new_field`);
```

---

## 对象映射规范
> 📚 参考知识库: `knowledge-base/code-specs/对象映射规范.md`

### DO ↔ Entity 映射
- **DO → Entity**: [转换逻辑说明]
- **Entity → DO**: [转换逻辑说明]

### Entity ↔ DTO 映射
- **Entity → DTO**: [转换逻辑说明]
- **DTO → Entity**: [转换逻辑说明]

### 特殊字段处理
- [字段名]: [特殊处理逻辑]

---

## 事务处理详解

### 事务边界
- **主事务**: `[AppService].[methodName]`
- **嵌套事务** [如需要]: `[Service].[methodName]`

### 事务传播策略
```java
@Transactional(
    rollbackFor = Exception.class,
    propagation = Propagation.REQUIRED
)
```

### 事务回滚规则
- 业务异常回滚: `BusinessException`
- 系统异常回滚: `Exception`

---

## 异常处理
> 📚 参考知识库: `knowledge-base/code-specs/异常处理规范.md`

### 业务异常
```java
throw new BusinessException("错误码", "错误信息");
```

### 系统异常
```java
throw new SystemException("错误信息");
```

### 异常处理位置
- **Domain层**: 抛出业务异常
- **Application层**: 捕获并转换异常
- **Adapter层**: 统一异常处理,返回标准响应

---

## 跨包/跨限界上下文调用
> 📚 参考知识库: `knowledge-base/code-specs/跨包调用规范.md`

### 跨限界上下文调用说明
[如果涉及跨限界上下文调用,说明调用方式和注意事项]

### 模块依赖关系
```
[当前模块] → [依赖模块1] → [依赖模块2]
```

---

## 日志记录规范

### 关键节点日志
- **方法入口**: `log.info("[ClassName].[methodName] 开始执行, 参数: {}", param)`
- **方法出口**: `log.info("[ClassName].[methodName] 执行完成, 结果: {}", result)`
- **异常日志**: `log.error("[ClassName].[methodName] 执行失败", exception)`

### 敏感信息脱敏
- 密码: [脱敏规则]
- 手机号: [脱敏规则]
- 身份证: [脱敏规则]

---

## 配置项 [如需要]

### application.yml 配置
```yaml
custom:
  config:
    key1: value1
    key2: value2
```

### Apollo 配置中心
- 配置项1: [说明]
- 配置项2: [说明]
